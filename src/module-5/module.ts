// import { add as addTwo, divider, multiply, subtract} from "./main"
// while card import
import * as Methods from "./main";

const add = (param1: number, param2:number, param3: number):number => {
    return param1 + param2 + param3
}

const res = Methods.add(20, 30);